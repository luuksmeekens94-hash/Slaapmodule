{
  "name": "fysionair-slaapmodule",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-module.mjs && npm test",
    "validate:live": "node scripts/validate-module.mjs --live && npm test",
    "build": "npm run validate",
    "vercel-build": "npm run validate",
    "serve": "node scripts/serve.mjs",
    "test:browser": "playwright test",
    "test:v2": "node --test tests/v2.test.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0"
  }
}
